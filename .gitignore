# ── Dependencies ───────────────────────────────
node_modules/
ui/node_modules/

# ── Build output ───────────────────────────────
ui/dist/
cloud-api
cloud-api-linux
*.exe

# ── Secrets / env (keep .env.example) ──────────
.env
.env.local
*.local.json

# ── Runtime data ───────────────────────────────
uploads/

# ── Data exports / logs ────────────────────────
*.xlsx
*.log

# ── OS / editor ────────────────────────────────
.DS_Store
*.swp

# ── Repo terpisah & perkakas lokal ─────────────
# cloud-nusantara punya repo sendiri (candradigitalindo/cloud-nusantara).
# Sengaja diabaikan agar tidak tersimpan sebagai gitlink kosong di sini.
cloud-nusantara/
.claude/
